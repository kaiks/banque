<% admin_view = true %>
<% users = local_assigns[:users]||@users %>
<table class="table table-striped table-responsive">
  <thead>
  <tr>
    <th>Prénom</th>
    <th>Nom</th>
    <th>Age</th>
    <th>Ville</th>
  </tr>
  </thead>

  <tbody>
  <% users.each do |user| %>
      <tr>
        <td><%= user.name%></td>
        <td><%= user.surname %></td>
        <td><%= user.age %></td>
        <td><%= user.city %></td>
      </tr>
  <% end %>
  </tbody>
</table>